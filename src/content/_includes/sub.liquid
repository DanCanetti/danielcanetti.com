<!doctype html>
<html lang="en">
    <head>
        {% include meta %}
        <title>Daniel Canetti • {{ pageTitle }}</title>
        <meta name="description" content="{{ pageDesc }}">
        {% include social %}
    </head>
    <body class="{{ pageClass }}">
        <div id="wrapper" v-bind:class="{ 'night' : nightMode }" class="wrapper">
            {% include nav %}
            <h1 class="--sub">{{ pageTitle }}</h1>
            {{ content }}
        </div>
        {% include footer %}
    </body>
</html>
