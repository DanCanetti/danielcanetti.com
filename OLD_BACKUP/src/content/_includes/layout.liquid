<!doctype html>
<html lang="en">
    <head>
        {% include meta %}
        <title>Daniel Canetti • {{ pageTitle }}</title>
        <meta name="description" content="{{ pageDesc }}">
        {% include social %}
    </head>
    <body class="{{ pageClass }}">
      <div id="wrapper" class="wrapper container max-w-screen-md m-auto">
        {% include nav %}
        {{ content }}
      </div>
      {% include footer %}
    </body>
</html>
