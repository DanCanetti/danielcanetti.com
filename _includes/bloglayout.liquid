<!doctype html>
<html lang="en">
    <head>
        {% include meta %}
        <title>Daniel Canetti • {{ pageTitle }}</title>
    </head>
    <body class="{{ pageClass }}">
      <div class="wrapper">
        {% include nav %}
        <div class="header">
          <h1>{{ pageTitle }}</h1>
    	    <p class="post-date">{{ postDate }}</p>
        </div>
        <div class="content">
          {{ content }}
        </div>
        {% include footer %}
      </div>
    </body>
</html>
