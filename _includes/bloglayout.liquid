<!doctype html>
<html lang="en">
    <head>
        {% include meta %}
        <title>Daniel Canetti • Blog • {{ postTitle }}</title>
    </head>
    <body class="{{ pageClass }}">
      <div class="wrapper">
        {% include nav %}
        <div id="top" class="header">
          <h1 class="title">{{ postTitle }}</h1>
          <span>{{ postDate }}</span>
          <img src="{{ postFeatureImage }}" />
        </div>
        <div class="content">
          {{ content }}
          <a href="#top" class="top"><img src="/images/top.svg" /></a>
        </div>
      </div>
      {% include footer %}
    </body>
</html>
