<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Daniel Canetti • {{ pageTitle }}</title>
    </head>
    <body>
        <h1>{{ pageTitle }}</h1>
	<p class="post-date">{{ postDate }}</p>
	
        {{ content }}
    </body>
</html>
